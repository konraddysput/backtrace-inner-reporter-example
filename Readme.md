# Backtrace inner reporter example

Backtrace example game with inner reporter example that stores a reference to Backtrace-Unity plugin.

## Goal:
* Validate if .java files are available in final android apk.